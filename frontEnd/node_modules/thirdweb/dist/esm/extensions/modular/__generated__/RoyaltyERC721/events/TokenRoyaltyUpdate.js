import { prepareEvent } from "../../../../../event/prepare-event.js";
/**
 * Creates an event object for the TokenRoyaltyUpdate event.
 * @param filters - Optional filters to apply to the event.
 * @returns The prepared event object.
 * @extension MODULAR
 * @example
 * ```ts
 * import { getContractEvents } from "thirdweb";
 * import { tokenRoyaltyUpdateEvent } from "thirdweb/extensions/modular";
 *
 * const events = await getContractEvents({
 * contract,
 * events: [
 *  tokenRoyaltyUpdateEvent({
 *  tokenId: ...,
 *  recipient: ...,
 * })
 * ],
 * });
 * ```
 */
export function tokenRoyaltyUpdateEvent(filters = {}) {
    return prepareEvent({
        signature: "event TokenRoyaltyUpdate(uint256 indexed tokenId, address indexed recipient, uint256 bps)",
        filters,
    });
}
//# sourceMappingURL=TokenRoyaltyUpdate.js.map